---
import Layout from './Layout.astro';
import DynamicComponent from '../components/DynamicComponent.astro';

interface Props {
  title: string;
  pageContent?: any[];
  backLinkText?: string;
  backLinkUrl?: string;
}

const {
  title,
  pageContent = [],
  backLinkText = "← Tilbake til forsiden",
  backLinkUrl = "/"
} = Astro.props;
---

<Layout title={`${title} - Sanity + Astro`}>
  <main class="container">
    <header class="page-header">
      <h1 class="page-title">
        {title}
      </h1>
    </header>

    <!-- Page content from schema -->
    {pageContent && Array.isArray(pageContent) && pageContent.length > 0 && (
      <div class="page-content">
        {pageContent.map((block) => (
          <DynamicComponent key={block._key || block._id} block={block} />
        ))}
      </div>
    )}

    <!-- Main content slot (for listings, etc.) -->
    <slot />

    <footer class="page-footer">
      <a href={backLinkUrl} class="back-link">
        {backLinkText}
      </a>
    </footer>
  </main>
</Layout>

<style>
  /* Page Layout */
  .page-header {
    margin-block-end: clamp(var(--space-5), 5vw, var(--space-8));
  }

  .page-title {
    margin: 0 0 var(--space-2) 0;
    color: var(--color-text-primary);
  }

  .page-content {
    margin-block-end: clamp(var(--space-6), 6vw, var(--space-9));
  }

  .page-footer {
    margin-block-start: clamp(var(--space-6), 6vw, var(--space-9));
    padding-block-start: var(--space-6);
    border-block-start: 1px solid var(--color-border);
  }

  .back-link {
    color: var(--color-primary-500);
    text-decoration: none;
    font-weight: var(--font-weight-medium);
  }

  .back-link:hover {
    text-decoration: underline;
  }
</style>